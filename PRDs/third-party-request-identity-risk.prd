# PRD: 第三方大模型请求身份识别风险加固

**文档版本**: v1.2
**创建日期**: 2026-06-22
**作者**: Mz
**状态**: 草稿

---

## 1. 背景

sub2api 在向 Claude、OpenAI、Gemini、Kiro 等第三方大模型发请求时，通过模拟官方 CLI 客户端的 HTTP 请求头（User-Agent、X-Stainless-*、Originator 等）来降低被识别的风险。

本次排查发现，**请求头层面没有直接泄露 sub2api 字样**，但存在若干"行为指纹"层面的风险，第三方服务可通过统计分析手段将请求关联到同一代理服务。

---

## 2. 问题范围

### 2.1 排查覆盖的模型/服务

| 服务 | 主要请求文件 |
|------|-------------|
| Claude / Anthropic | `backend/internal/pkg/claude/constants.go` |
| OpenAI / Codex | `backend/internal/service/openai_gateway_service.go` |
| Gemini | `backend/internal/pkg/geminicli/` |
| Kiro | `backend/internal/pkg/kiro/fingerprint.go` |

### 2.2 不在本 PRD 范围内

- `admin_auth.go` 中 `Sec-WebSocket-Protocol: sub2api-admin` 是内部鉴权用，不对外暴露，不纳入。

---

## 3. 风险点详述

### 风险 R1：OpenAI Session_ID / Conversation_ID 固定派生

**级别**: 高
**文件**: `backend/internal/service/account_test_service.go:910-911`

```go
probeSessionID := compactProbeSessionID(account.ID)
req.Header.Set("Session_ID", probeSessionID)
req.Header.Set("Conversation_ID", probeSessionID)
```

**问题**: `Session_ID` 和 `Conversation_ID` 由 `account.ID` 固定派生，同一账户每次请求值相同。OpenAI 侧可通过该字段识别出"同一个 Session 发起了大量不同对话"，行为特征明显异常。

**影响**: OpenAI 可将该账户标记为异常、限流或封禁。

**建议修复**: 每次请求按真实会话周期轮换。

---

### 风险 R2：Claude User-Agent 版本未跟进官方更新

**级别**: 中
**文件**: `backend/internal/pkg/claude/constants.go:96`

```go
"User-Agent": "claude-cli/" + CLICurrentVersion + " (external, cli)",
```

**问题**: `CLICurrentVersion` 是硬编码常量。官方 Claude CLI 持续迭代，若 sub2api 使用的版本明显落后于当前市场版本，Anthropic 可在统计层面识别出"大量请求来自同一旧版本"。

**影响**: 版本过旧的 UA 可能触发 Anthropic 的风控规则，导致 OAuth Token 被限制。

**建议修复**: 建立 CLI 版本监控机制，官方更新后及时同步 `CLICurrentVersion` 及 `X-Stainless-Package-Version` 等相关常量。

---

### 风险 R3：Kiro fingerprint hash 每账户固定不变

**级别**: 中
**文件**: `backend/internal/pkg/kiro/fingerprint.go:214`

```go
func BuildKiroIDERuntimeUserAgent(accountKey, machineID string) string {
    fp := globalRuntimeFingerprints().Get(accountKey, machineID)
    return fmt.Sprintf("KiroIDE %s %s", kiroIDEUserAgentVersion, fp.KiroHash)
}
```

**问题**: `KiroHash` 基于 `accountKey + machineID` 派生，进程生命周期内固定。AWS/Kiro 后端可用该 hash 作为设备指纹，长期追踪同一账户的所有请求。

**影响**: Kiro 后端可能检测到"同一设备指纹发起了异常高频请求"，触发限流或封禁。

**建议修复**: 评估是否可按时间窗口或会话周期轮换 KiroHash，同时保持单次会话内的一致性。

---

### 风险 R4：X-Stainless-* 头部参数固定不变

**级别**: 低
**文件**: `backend/internal/pkg/claude/constants.go:97-104`


```go
"X-Stainless-OS":              "Linux",
"X-Stainless-Arch":            "arm64",
"X-Stainless-Runtime-Version": "v24.3.0",
```

**问题**: 所有账户使用完全相同的 OS、Arch、Runtime 组合，在统计上表现为"大量请求来自完全相同的环境"。

**影响**: Anthropic 可通过字段分布异常识别批量代理行为。

**场景分析**:

| 出口 IP 情况 | 是否随机化 OS/Arch | 风险评估 |
|------------|-----------------|---------|
| 有代理，IP 多样 | 随机化 | 推荐：IP 不同 + UA 不同，模拟多台真实设备 |
| 无代理，固定服务器 IP | 随机化 | **更危险**：同一 IP 出现多种"设备"，真实用户不会这样，反而是更强的异常信号 |
| 无代理，固定服务器 IP | 保持固定 | 可接受：表现为"一台固定机器"，与 CI/服务器场景一致 |

**建议修复**: 随机化策略与代理配置联动——账户配置了代理时启用 OS/Arch 随机化，未配置代理时保持固定值，避免同 IP 下出现"设备漂移"。

---

---

### 风险 R5：x-anthropic-billing-header 泄露客户端版本

**级别**: 高
**来源**: 社区逆向分析（[claude-code billing header 分析](https://gist.github.com/NTT123/579183bdd7e028880d06c8befae73b99)）

**问题**: Claude Code 在每次请求中注入 `x-anthropic-billing-header`，格式为：
```
x-anthropic-billing-header: cc_version=2.1.185.xxx; cc_entrypoint=cli;
```
该 header 由客户端计算生成，包含 CLI 版本和入口点信息。Anthropic 使用它做计费验证和使用来源追踪。sub2api 如果伪造或跳过该字段，或者版本号与 `User-Agent` 中的 CLI 版本不一致，就会形成矛盾信号，触发异常检测。

**影响**: Anthropic 可通过 billing header 与 UA 版本的不一致性识别出非官方客户端。

**建议修复**: 确保 `x-anthropic-billing-header` 与 `User-Agent` 中的 `CLICurrentVersion` 保持严格一致，版本更新时同步更新两处。

---

### 风险 R6：TLS 握手指纹（JA3/JA4）与宣称客户端不符

**级别**: 中
**来源**: [HTTP/2 and HTTP/3 Fingerprinting — Scrapfly](https://scrapfly.io/blog/posts/http2-http3-fingerprinting-guide)、[TLS-Fingerprint-API](https://github.com/Carcraftz/TLS-Fingerprint-API)

**问题**: TLS ClientHello 包在 HTTP 层之前就已被服务端记录，其中的加密套件顺序、扩展列表、GREASE 值等参数会生成 JA3/JA4 指纹。Go 标准库（`net/http`）的 TLS 指纹与 Node.js（真实 Claude CLI 的运行时）截然不同。sub2api 用 Go 发出的请求，UA 宣称是 Node.js 运行时，但 TLS 层已经暴露了实际是 Go 客户端。

**影响**: Anthropic 或其 CDN（如 Cloudflare）可在网络层识别出"伪装成 Node.js 的 Go 客户端"，无需分析任何 HTTP header。

**建议修复**: 评估引入 [`utls`](https://github.com/refraction-networking/utls) 库来模拟 Chrome/Node.js 的 TLS ClientHello 指纹；或在短期内评估实际风险优先级，因为 Anthropic 当前是否主动做 JA3 检测尚不确定。

---

### 风险 R7：Anthropic 对第三方 Harness 的 Git 内容扫描

**级别**: 中
**来源**: [MindStudio — Anthropic harness detection analysis](https://www.mindstudio.ai/blog/anthropic-harness-detection-git-commit-billing-overcharge)

**问题**: Anthropic 会扫描请求上下文（包括 system prompt 和 git commit 内容）中的关键词，如 `hermes.md`、`OpenClaw`、`sub2api` 等第三方 harness 标识。一旦检测到，账号会被切换至 API 计费模式或触发审查。

**影响**: 若用户在 Claude Code 会话中提交了含有 `sub2api` 字样的 commit message 或在 system prompt 里带有相关标识，可能导致账号被识别和计费切换。

**建议修复**: 这是用户行为层面的风险，sub2api 本身无法完全规避；可在文档中提示用户避免在 Claude Code 会话内容中出现 `sub2api` 等关键词。

---

### 风险 R8：Claude Code 遥测缺失暴露代理特征

**级别**: 低
**来源**: [cc-gateway README 分析](https://raw.githubusercontent.com/motiful/cc-gateway/main/README.md)

**问题**: 真实 Claude Code CLI 会持续上报 640+ 种遥测事件（设备 ID、OS 版本、Shell 类型、CPU 架构、内存等 40+ 环境维度）。sub2api 作为纯 API 代理，不产生任何遥测上报。若 Anthropic 将"有 API 请求但无遥测"视为异常信号，则可识别出代理行为。

**影响**: 相对间接，Anthropic 需要主动将遥测与 API 流量关联才能触发，当前风险较低。

**建议**: 暂不处理，持续观察 Anthropic 风控策略演进。

---

## 4. 风险优先级总览

| 编号 | 风险描述 | 级别 | 建议处理时机 |
|------|---------|------|------------|
| R1 | OpenAI Session_ID 固定派生 | 高 | 尽快修复 |
| R5 | x-anthropic-billing-header 版本不一致 | 高 | 与 R2 同步处理 |
| R2 | Claude UA 版本未跟进 | 中 | 建立版本监控流程 |
| R3 | Kiro fingerprint hash 固定 | 中 | 下一迭代评估 |
| R6 | TLS 指纹与宣称运行时不符 | 中 | 评估实际触发概率后决策 |
| R7 | Git/system prompt 内容扫描 | 中 | 用户文档提示 |
| R4 | X-Stainless-* 随机化需与代理联动 | 低 | 低优先级（需代理改造配合） |
| R8 | 遥测缺失暴露代理特征 | 低 | 暂观察 |

---

## 5. 当前未发现的问题（已确认安全）

- 请求头中**无 sub2api 字样**泄露给第三方 API。
- Claude、OpenAI、Kiro 均使用对应官方客户端的 UA 格式，单次请求无法直接识别为代理服务。
- Kiro 使用了随机 `Amz-Sdk-Invocation-Id`（`uuid.NewString()`），单次请求层面无固定标识。

---

## 6. 后续行动

- [ ] R1：修改 `compactProbeSessionID` 逻辑，改为随机生成或按会话轮换
- [ ] R2 + R5：建立 Claude CLI 版本跟踪机制，`CLICurrentVersion`、`x-anthropic-billing-header`、`X-Stainless-Package-Version` 三处同步更新
- [ ] R3：调研 Kiro hash 轮换方案，评估对会话一致性的影响
- [ ] R4：X-Stainless-* 随机化与代理配置联动，有代理时随机化，无代理时保持固定
- [ ] R6：调研 `utls` 库接入成本，评估 Anthropic 是否实际执行 JA3 检测
- [ ] R7：在用户文档中增加提示，避免在 Claude Code 会话内容中出现 `sub2api` 等关键词

---

## 7. 验证方法（每项改动上线前必做）

> 改动质量的底线：**没有通过验证的修复不比不改强**。以下方法对应各风险点，改动完成后必须完成对应验证才能视为完成。

### 7.1 Header 层验证（R1、R2、R4、R5）

使用 mitmproxy 同时抓取真实 CLI 流量和 sub2api 流量，做逐字段 diff：

```powershell
# 启动 mitmproxy
mitmproxy --listen-port 8080 --save-stream-file capture.mitm

# 让 sub2api 走本地代理
$env:HTTPS_PROXY = "http://127.0.0.1:8080"
```

验收标准：sub2api 的请求 headers 与真实 CLI 抓包结果无非预期差异。

对于 **R1 Session_ID 随机化**，必须补单元测试：

```go
// 同一账户连续两次生成，值必须不同
assert.NotEqual(t, buildSessionID(account), buildSessionID(account))
// 同一会话内多次调用，值必须稳定
sid := buildSessionID(account)
assert.Equal(t, sid, buildSessionID(account))
```

对于 **R5 版本一致性**，在启动时加自检断言，防止版本漂移上线：

```go
// 进程启动时执行，确保两处版本号一致
billingVersion := extractVersionFromBillingHeader(DefaultBillingHeader)
if billingVersion != CLICurrentVersion {
    panic("billing header version mismatch with CLICurrentVersion")
}
```

### 7.2 TLS 指纹验证（R6）

用 Go 客户端和真实 Node.js 分别访问 [https://tls.peet.ws/api/all](https://tls.peet.ws/api/all)，对比 JA3/JA4 字段：

```powershell
# Node.js 基准值
node -e "const https = require('https'); https.get('https://tls.peet.ws/api/all', r => r.pipe(process.stdout))"

# Go 客户端实际值（通过 sub2api 发出）
curl --proxy http://127.0.0.1:8080 https://tls.peet.ws/api/all
```

验收标准：引入 `utls` 后，Go 客户端的 JA3 hash 与 Node.js 基准值一致。

### 7.3 行为层整体验证

**影子流量对比**：改动前后各发 10 次请求到 [https://httpbin.org/headers](https://httpbin.org/headers)，记录完整快照，确认改动范围符合预期、没有引入新的异常字段。

**金丝雀账号观察**：
- 每次重大改动后，先在 2-3 个低价值测试账号上运行 24-48 小时
- 观察封禁率、限流率是否异常
- 确认正常后再推广到正式账号池

---

## 8. 上游合并兼容原则

本项目需要定期合并上游（Wei-Shaw/sub2api）的更新。所有反侦测改动必须遵循以下原则，避免合并冲突或被上游覆盖。

### 8.1 改动隔离策略

**禁止直接修改上游文件中的常量**。应通过覆盖或注入的方式实现，将改动集中在独立文件中。

| 风险 | 错误做法 | 正确做法 |
|------|---------|---------|
| R1 Session_ID | 直接改 `account_test_service.go` 中的生成逻辑 | 抽取 `session_id_builder.go`，通过接口注入 |
| R2/R5 版本号 | 直接改 `constants.go` 中的 `CLICurrentVersion` | 在独立的 `version_override.go` 中覆盖，版本更新只改这一处 |
| R4 X-Stainless-* | 直接改 `DefaultHeaders` map | 新增 `BuildHeaders(account) map[string]string`，在调用侧替换 |

### 8.2 合并冲突处理规则

1. **上游改了 constants.go**：检查新版本号，同步更新 `version_override.go`，以上游为准
2. **上游改了 Session_ID 生成逻辑**：评估上游改动是否已经解决了 R1，若已解决则撤销本地改动，避免双重处理
3. **上游改了 fingerprint.go**：重新评估 R3，以上游最新逻辑为基准

### 8.3 合并后必做的回归验证

每次合并上游后，按 **第 7 节** 重新执行一遍 Header 层验证，确认上游改动没有破坏已有的反侦测逻辑，且没有引入新的版本不一致。
